{
  "name": "hiddenplayer",
  "scripts": {
    "clean": "rm -rf ./modules",
    "compile": "npm run clean && tsc",
    "test": "npm run compile && reciple",
    "start": "reciple"
  },
  "devDependencies": {
    "@types/node": "^17.0.24",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@ghextercortes/djs-pagination": "^0.0.5",
    "better-sqlite3": "^7.5.1",
    "reciple": "^1.0.10"
  }
}

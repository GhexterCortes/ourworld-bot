{
  "name": "hiddenplayer",
  "scripts": {
    "clean": "rm -rf ./modules",
    "compile": "yarn run clean && tsc",
    "test": "yarn run compile && yarn run start",
    "start": "reciple"
  },
  "devDependencies": {
    "@types/node": "^17.0.24",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@ghextercortes/djs-pagination": "^0.0.5",
    "better-sqlite3": "^7.5.1",
    "minecraft-protocol": "^1.33.0",
    "reciple": "^1.0.11",
    "string-similarity": "^4.0.4",
    "yaml": "^2.0.1"
  }
}
